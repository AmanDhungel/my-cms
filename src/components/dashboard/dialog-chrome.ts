/**
 * The style guide's modal: a warm dark scrim, an off-white panel with a real
 * border and a deep shadow. The shadcn default is a much lighter treatment,
 * so every EMS dialog opts into these instead.
 */
export const emsDialogOverlay = "bg-[rgba(27,24,21,0.45)] backdrop-blur-none"

export const emsDialogContent =
  "border-n-300 bg-n-50 text-n-900 rounded-2xl border p-0 ring-0 shadow-[0_24px_60px_rgba(27,24,21,0.3)]"
